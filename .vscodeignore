# Все зависимости инлайнятся esbuild в бандлы (external только vscode);
# в рантайме с диска читаются лишь out/tree-sitter.wasm и out/wasm/*.wasm.
node_modules/
# Локальное состояние: граф-БД проекта и другие служебные каталоги
.ntgraph/
.neuraltower/
.vscode-test/
out/**/*.js.map
ref/
src/
*.bat
bump-version.js
.github/
.*
*.md 
!README.md
UI-UX/**/*
plan/**/*
docs/**/*
