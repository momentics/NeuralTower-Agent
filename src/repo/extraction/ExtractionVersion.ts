/**
 * Версия экстракции.
 *
 * Монотонно возрастающее целое, идентифицирующее *форму и глубину*
 * извлечённых данных. В отличие от CURRENT_SCHEMA_VERSION (которая
 * отслеживает структуру SQLite), эта версия отслеживает СОДЕРЖИМОЕ
 * экстракции — виды узлов, рёбра, синтезаторы, охват разрешений.
 *
 * Когда индекс был построен старым движком с более низкой версией,
 * данные на диске структурно корректны, но УСТАРЕЛИ: им не хватает
 * того, что новый экстрактор теперь производит. Миграция схемы не
 * может это восстановить — только повторная индексация.
 */
export const EXTRACTION_VERSION = 1;
